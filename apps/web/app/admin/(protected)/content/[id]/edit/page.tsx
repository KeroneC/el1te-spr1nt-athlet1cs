import { ContentBlockForm } from "@/components/admin/content-block-form"; import { PageHeader } from "@/components/admin/page-header"; import { getAdminItem } from "@/lib/admin/page-data"; import type { AdminContentBlock } from "@/lib/admin/types";
export default async function Page({ params, searchParams }: { params: Promise<{id:string}>; searchParams: Promise<{saved?:string}> }) { const {id}=await params; const item=await getAdminItem<AdminContentBlock>(`/api/admin/content-blocks/${encodeURIComponent(id)}`); const {saved}=await searchParams; return <><PageHeader title="Edit content block" description={item.key} />{saved && <p role="status" className="mb-5 border-l-4 border-track-field bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-900">Content block created successfully.</p>}<ContentBlockForm item={item} /></>; }
