import { SponsorForm } from "@/components/admin/sponsor-form"; import { PageHeader } from "@/components/admin/page-header"; import { getAdminItem } from "@/lib/admin/page-data"; import type { AdminSponsor } from "@/lib/admin/types";
export default async function Page({ params, searchParams }: { params: Promise<{id:string}>; searchParams: Promise<{saved?:string}> }) { const {id}=await params; const item=await getAdminItem<AdminSponsor>(`/api/admin/sponsors/${encodeURIComponent(id)}`); const {saved}=await searchParams; return <><PageHeader title="Edit sponsor" description={`Manage /${item.slug}`} />{saved && <p role="status" className="mb-5 border-l-4 border-track-field bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-900">Sponsor created successfully.</p>}<SponsorForm item={item} /></>; }
