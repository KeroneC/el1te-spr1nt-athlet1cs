import { EventForm } from "@/components/admin/event-form"; import { PageHeader } from "@/components/admin/page-header"; import { getAdminItem } from "@/lib/admin/page-data"; import type { AdminEvent } from "@/lib/admin/types";
export default async function Page({ params, searchParams }: { params: Promise<{id:string}>; searchParams: Promise<{saved?:string}> }) { const {id}=await params; const item=await getAdminItem<AdminEvent>(`/api/admin/events/${encodeURIComponent(id)}`); const {saved}=await searchParams; return <><PageHeader title="Edit event" description={`Manage /${item.slug}`} />{saved && <p role="status" className="mb-5 border-l-4 border-track-field bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-900">Event created successfully.</p>}<EventForm item={item} /></>; }
