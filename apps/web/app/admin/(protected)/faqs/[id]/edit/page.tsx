import { FaqForm } from "@/components/admin/faq-form"; import { PageHeader } from "@/components/admin/page-header"; import { getAdminItem } from "@/lib/admin/page-data"; import type { AdminFaq } from "@/lib/admin/types";
export default async function Page({ params, searchParams }: { params: Promise<{id:string}>; searchParams: Promise<{saved?:string}> }) { const {id}=await params; const item=await getAdminItem<AdminFaq>(`/api/admin/faqs/${encodeURIComponent(id)}`); const {saved}=await searchParams; return <><PageHeader title="Edit FAQ" description={item.category} />{saved && <p role="status" className="mb-5 border-l-4 border-track-field bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-900">FAQ created successfully.</p>}<FaqForm item={item} /></>; }
